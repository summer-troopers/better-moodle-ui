import { StudentRoleDirective } from './student-role.directive';

describe('StudentRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new StudentRoleDirective();
    expect(directive).toBeTruthy();
  });
});
