import { PagePreloaderDirective } from './page-preloader.directive';

describe('PagePreloaderDirective', () => {
  it('should create an instance', () => {
    const directive = new PagePreloaderDirective();
    expect(directive).toBeTruthy();
  });
});
