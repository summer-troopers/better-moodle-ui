import { TeacherRoleDirective } from './teacher-role.directive';

describe('TeacherRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new TeacherRoleDirective();
    expect(directive).toBeTruthy();
  });
});
